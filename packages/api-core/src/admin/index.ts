/**
 * @giulio-leone/api-admin
 *
 * API routes per admin (users, feature-flags, policies, invitations, metadata, affiliates, promotions, config, etc.)
 *
 * NOTE: Le route Next.js vengono esportate direttamente dalle loro posizioni in apps/next/app/api/
 * Questo file esporta solo i tipi e utility condivisi.
 */

// Export types and utilities only, not route handlers
// Route handlers should be imported directly where needed

export {};
